#include <stdio.h>
/**
 * main - Entry point
 *  learning puts function
 * Return: Always 0 (success)
 */

int main(void)
{
		char tx[] = "\"Programming is like building a multilingual puzzle";

			puts(tx);
				return (0);
}
